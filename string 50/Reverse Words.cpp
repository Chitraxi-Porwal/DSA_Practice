#include <bits/stdc++.h>
using namespace std;

reserveWords(string str){
    vector<string> words;
    stringstream ss
}


int main() {
    string str = "..geeks..for..geeks.";
    cout << reserveWords(str) << endl;
    return 0;
}
    if (!word.empty()) {
        words.push_back(word);
    } 